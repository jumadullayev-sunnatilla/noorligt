import React, { memo } from "react";
import SWIPER from "../../component/swiper/SWIPER.jsx";

const Home = () => {
  return (
    <div>
      <SWIPER />
    </div>
  );
};

export default memo(Home);
